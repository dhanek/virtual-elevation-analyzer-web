# Revisions to skip when running `git blame`. Mechanical, whole-tree changes
# only — a commit belongs here when it changed how the code is WRITTEN and not
# what it does, so that blame keeps pointing at whoever last changed the
# meaning of a line.
#
# Use it locally with:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
# GitHub applies it automatically.

# style: reformat with prettier (2026-09-07) — 166 files, no behavioural change.
fa35adebba3bc14a84d28530907258b127582ae7
