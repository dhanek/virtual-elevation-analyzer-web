---
phase: 04-gps-and-out-and-back-shell-extraction
plan: 03
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified:
  - scripts/validate-ui-shell-guardrails.sh
autonomous: false
requirements: [BEHV-03, BEHV-04]

must_haves:
  truths:
    - "GPS in-place updates preserve active tab and scroll position during auto-adjust and slider changes (BEHV-03)"
    - "GPS lap, GPS gate one-way, and out-and-back modes retain correct air-speed calibration behavior after shell extraction (BEHV-04)"
    - "All CI checks (typecheck, lint, test, build) pass after extraction"
    - "Guardrail validation script passes"
  artifacts:
    - path: "scripts/validate-ui-shell-guardrails.sh"
      provides: "Phase 4 extraction validation"
  key_links:
    - from: "frontend/src/shell/gpsLap/updateGpsLap.ts"
      to: "frontend/src/shell/dom/tabs.ts"
      via: "setupTabSwitching called during in-place update preserves active tab"
      pattern: "setupTabSwitching"
    - from: "frontend/src/shell/outAndBack/updateOutAndBack.ts"
      to: "frontend/src/shell/dom/tabs.ts"
      via: "setupTabSwitching called during in-place update preserves active tab"
      pattern: "setupTabSwitching"
    - from: "frontend/src/shell/gpsLap/renderGpsLap.ts"
      to: "frontend/src/analysis/MultiSegmentSettings.ts"
      via: "buildAutoCalibrationSegmentsFromRanges for auto-adjust calibration"
      pattern: "buildAutoCalibrationSegmentsFromRanges"
    - from: "frontend/src/shell/outAndBack/renderOutAndBack.ts"
      to: "frontend/src/analysis/MultiSegmentSettings.ts"
      via: "buildAutoCalibrationSegmentsFromRanges for auto-adjust calibration"
      pattern: "buildAutoCalibrationSegmentsFromRanges"
---

<objective>
Validate that BEHV-03 (tab/scroll preservation during in-place GPS updates) and BEHV-04 (air-speed calibration correctness across GPS modes) survived the GPS-lap and out-and-back shell extraction. Run automated CI checks and present a manual browser verification checkpoint.

Purpose: Confirm behavioral invariants are intact before closing Phase 4. The manual browser checklist is authoritative for BEHV-03 and BEHV-04 per D-15.
Output: Passing CI, passing guardrails, and human verification of GPS behavior in the browser.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-gps-and-out-and-back-shell-extraction/04-CONTEXT.md
@.planning/phases/04-gps-and-out-and-back-shell-extraction/04-UI-SPEC.md

# Prior plan summaries
@.planning/phases/04-gps-and-out-and-back-shell-extraction/04-01-SUMMARY.md
@.planning/phases/04-gps-and-out-and-back-shell-extraction/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full automated validation suite and structural verification</name>
  <files>
    scripts/validate-ui-shell-guardrails.sh
  </files>
  <read_first>
    scripts/validate-ui-shell-guardrails.sh
    docs/testing/ui-shell-regression-contract.md
    docs/testing/ui-shell-manual-checklist.md
    frontend/src/main.ts (scan for any remaining GPS-lap or out-and-back function definitions that should have been removed)
    frontend/src/shell/gpsLap/updateGpsLap.ts (verify updateGpsLapVEPlots contains tab/scroll save-restore logic)
    frontend/src/shell/outAndBack/updateOutAndBack.ts (verify updateOutAndBackVEPlots contains tab/scroll save-restore logic)
    frontend/src/shell/gpsLap/renderGpsLap.ts (verify showGpsLapVEPlot contains auto-adjust calibration wiring with buildAutoCalibrationSegmentsFromRanges)
    frontend/src/shell/outAndBack/renderOutAndBack.ts (verify showOutAndBackVEPlot contains auto-adjust calibration wiring with buildAutoCalibrationSegmentsFromRanges)
    frontend/src/analysis/MultiSegmentSettings.ts (verify all 4 persistence helpers present)
  </read_first>
  <action>
**1. Run the full guardrail validation script:**
```bash
bash scripts/validate-ui-shell-guardrails.sh
```
This runs `cargo test --lib`, `wasm-pack build`, `npm run check`, `npm run lint`, `npm run test`, `npm run build` per D-13/D-14.

**2. Verify structural extraction completeness:**
Confirm main.ts does NOT contain any of these function definitions:
- `showGpsLapVEAnalysis`, `showGpsLapVEPlot`, `setupGpsLapSliderHandlers`, `recalculateGpsLapVE`, `saveGpsLapScreenshot`, `renderGpsLapVEPlots`, `renderGpsLapWindPlot`, `renderGpsLapPowerPlot`, `renderGpsLapVdPlot`, `updateGpsLapVEPlots`, `calculateGpsLapStats`, `calculateMeanElevationProfile`, `getGpsLapNumberForRange`
- `showOutAndBackVEAnalysis`, `showOutAndBackVEPlot`, `setupOutAndBackSliderSync`, `recalculateOutAndBackVE`, `saveOutAndBackScreenshot`, `renderOutAndBackPlots`, `renderOutAndBackWindPlot`, `renderOutAndBackPowerPlot`, `renderOutAndBackVdPlot`, `updateOutAndBackVEPlots`, `calculateOutAndBackStats`, `calculateOutAndBackMeanElevation`, `buildOutAndBackMultiSegmentSeries`
- `resolveMultiSegmentAnalysisParams`, `saveCurrentMultiSegmentSettings`, `saveMapTrimSettings`, `buildAutoCalibrationSegmentsFromRanges`
- `MULTI_SEGMENT_COLORS`, `getMultiSegmentColor`, `interpolateElevation`
- `LapVEProfile`, `OutAndBackVEProfile`

**3. Verify BEHV-03 structural invariant:**
Confirm `updateGpsLapVEPlots` in `shell/gpsLap/updateGpsLap.ts` contains the tab/scroll save-restore pattern. Look for: reading the currently active tab button before re-render, calling `setupTabSwitching` after re-render, and re-activating the saved tab. Same check for `updateOutAndBackVEPlots` in `shell/outAndBack/updateOutAndBack.ts`.

**4. Verify BEHV-04 structural invariant:**
Confirm the calibration chain is intact:
- `shell/gpsLap/renderGpsLap.ts` calls `buildAutoCalibrationSegmentsFromRanges` from `../../analysis/MultiSegmentSettings`
- `shell/outAndBack/renderOutAndBack.ts` calls `buildAutoCalibrationSegmentsFromRanges` from `../../analysis/MultiSegmentSettings`
- `analysis/MultiSegmentSettings.ts` exports `buildAutoCalibrationSegmentsFromRanges` with explicit `appState` parameter
- Both render modules import `calculateAutoAirSpeedCalibrationPercent` from `../../analysis/AirSpeedCalibration`

**5. Report main.ts line count reduction:**
Run `wc -l frontend/src/main.ts` and compare to baseline (4776 lines before Phase 4). Report the reduction.
  </action>
  <verify>
    <automated>cd /Users/hannes/Documents/git/virtual-elevation-analyzer-web && bash scripts/validate-ui-shell-guardrails.sh</automated>
  </verify>
  <acceptance_criteria>
    - scripts/validate-ui-shell-guardrails.sh exits 0
    - grep -c "function showGpsLapVEAnalysis\|function showOutAndBackVEAnalysis\|function updateGpsLapVEPlots\|function updateOutAndBackVEPlots" frontend/src/main.ts returns 0
    - grep -c "buildAutoCalibrationSegmentsFromRanges" frontend/src/shell/gpsLap/renderGpsLap.ts returns at least 1
    - grep -c "buildAutoCalibrationSegmentsFromRanges" frontend/src/shell/outAndBack/renderOutAndBack.ts returns at least 1
    - grep -c "setupTabSwitching" frontend/src/shell/gpsLap/updateGpsLap.ts returns at least 1
    - grep -c "setupTabSwitching" frontend/src/shell/outAndBack/updateOutAndBack.ts returns at least 1
    - wc -l frontend/src/main.ts shows materially fewer lines than 4776
  </acceptance_criteria>
  <done>All automated CI checks pass. Structural verification confirms GPS-lap and out-and-back functions fully extracted. Tab/scroll and calibration invariant code present in extracted modules. main.ts materially reduced.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual browser verification of BEHV-03 and BEHV-04</name>
  <files>docs/testing/ui-shell-manual-checklist.md</files>
  <action>
Present the manual browser verification checklist to the user. Start the dev server with `cd frontend && npm run dev`. The user will verify BEHV-03 (tab/scroll preservation) and BEHV-04 (calibration correctness) in the browser using the steps listed below. No code changes in this task -- verification only.
  </action>
  <verify>User confirms "approved" after completing all 17 verification steps below.</verify>
  <done>User has verified BEHV-03 and BEHV-04 in the browser and approved.</done>
  <what-built>GPS-lap and out-and-back shell code extracted from main.ts into shell/gpsLap/ and shell/outAndBack/ modules. All analysis behavior should be identical to pre-extraction. This checkpoint verifies the two highest-risk behavioral invariants that cannot be tested automatically.</what-built>
  <how-to-verify>
Start the dev server: `cd frontend && npm run dev`

**BEHV-03 -- Tab/Scroll Preservation (GPS-lap mode):**
1. Load a FIT file with GPS lap data
2. Select GPS Lap mode and run analysis
3. Click the "Wind" tab to switch from the default VE tab
4. Move the CdA slider -- verify the Wind tab stays active (does NOT flip to VE tab)
5. Move the Crr slider -- verify the Wind tab stays active
6. Click "Auto Adjust" -- verify the Wind tab stays active
7. Verify scroll position does not jump during any of the above

**BEHV-03 -- Tab/Scroll Preservation (Out-and-back mode):**
8. Load a FIT file with out-and-back data (or use the same file if it has out-and-back sections)
9. Select Out-and-Back mode and run analysis
10. Click the "Power" tab
11. Move the CdA slider -- verify the Power tab stays active
12. Click "Auto Adjust" -- verify the Power tab stays active
13. Verify scroll position does not jump

**BEHV-04 -- Air-speed Calibration Correctness:**
14. In GPS-lap mode: click "Auto Adjust" and verify the air-speed calibration slider updates to a non-zero value
15. Change the calibration slider manually, re-run analysis, verify VE plots update with the new calibration
16. In Out-and-back mode: click "Auto Adjust" and verify calibration updates
17. Verify that switching between modes preserves per-mode saved settings (CdA, Crr, calibration)

**Expected:** All behaviors identical to before Phase 4 extraction. No tab flipping, no scroll jumping, calibration works correctly in all GPS modes.
  </how-to-verify>
  <resume-signal>Type "approved" if all BEHV-03 and BEHV-04 checks pass, or describe specific issues observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundary changes. Validation-only plan.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | -- | -- | -- | No new code introduced in this plan. Validation only. |
</threat_model>

<verification>
1. `bash scripts/validate-ui-shell-guardrails.sh` passes (all CI checks)
2. main.ts does not contain GPS-lap or out-and-back function definitions
3. Tab/scroll preservation verified in browser for both modes
4. Air-speed calibration verified in browser for GPS-lap and out-and-back
5. Human approves BEHV-03 and BEHV-04 checkpoint
</verification>

<success_criteria>
Phase 4 behavioral invariants confirmed intact: BEHV-03 (tab/scroll preservation) and BEHV-04 (calibration correctness) verified through automated structural checks and manual browser testing. All CI passes. Phase 4 ready for closeout.
</success_criteria>

<output>
After completion, create `.planning/phases/04-gps-and-out-and-back-shell-extraction/04-03-SUMMARY.md`
</output>
