---
phase: 03-section-3-and-standard-ve-shell-extraction
plan: 03
type: execute
wave: 3
depends_on: ["01", "02"]
files_modified:
  - frontend/src/main.ts
autonomous: true
requirements:
  - BEHV-01
  - BEHV-02
must_haves:
  truths:
    - Auto-scroll to Analysis Parameters after a successful FIT or CSV file load is preserved.
    - Standard VE analysis behaves identically to before the extraction.
  artifacts:
    - path: frontend/src/main.ts
      provides: Preserved processFitFile and processCsvFile
      min_lines: 50
  key_links:
    - from: frontend/src/main.ts
      to: frontend/src/shell/ve/renderStandardVe.ts
      via: imports
---

<objective>
Re-integrate preserved standard-mode behaviors and checkpoint validation.
</objective>

<execution_context>
@.pi/gsd/workflows/execute-plan.md
@.pi/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-section-3-and-standard-ve-shell-extraction/03-CONTEXT.md
@.planning/phases/03-section-3-and-standard-ve-shell-extraction/03-RESEARCH.md
@frontend/src/main.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Verify and solidify auto-scroll navigation</name>
  <files>frontend/src/main.ts</files>
  <read_first>frontend/src/main.ts</read_first>
  <action>
In `frontend/src/main.ts`:
- Ensure `processFitFile` and `processCsvFile` still call `scrollToSection('parametersSection')` after loading a file and activating Section 2.
- Add any missing integration glue between `main.ts` and the newly extracted Section 3 / VE shell modules to make sure `handleAnalyze` triggers standard rendering seamlessly.
  </action>
  <verify>
    <automated>cd frontend && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/main.ts` contains `scrollToSection('parametersSection')` in `processFitFile`
    - `frontend/src/main.ts` contains `scrollToSection('parametersSection')` in `processCsvFile`
  </acceptance_criteria>
  <done>Navigation behaviors preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Run full integration and guardrail checks</name>
  <files>frontend/src/main.ts</files>
  <read_first>frontend/src/main.ts</read_first>
  <action>
Run the full UI-shell guardrails script to verify that no CI/WASM breakages occurred during the major extractions of Phase 3.
Update `bash scripts/report-ui-shell-hotspots.sh` if any line numbers shifted significantly and save the output.
  </action>
  <verify>
    <automated>bash scripts/validate-ui-shell-guardrails.sh --ci-only</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/validate-ui-shell-guardrails.sh --ci-only` passes
  </acceptance_criteria>
  <done>Phase 3 regressions avoided.</done>
</task>
</tasks>

<verification>
- [ ] Verify `validate-ui-shell-guardrails.sh --ci-only` passes.
</verification>

<success_criteria>
- BEHV-01 and BEHV-02 are satisfied.
- CI pipeline is green.
</success_criteria>

<output>
After completion, create .planning/phases/03-section-3-and-standard-ve-shell-extraction/03-03-SUMMARY.md
</output>
