---
phase: 03-section-3-and-standard-ve-shell-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/main.ts
  - frontend/src/shell/section3/bindGpsDetection.ts
  - frontend/src/shell/section3/bindOutAndBackDetection.ts
  - frontend/src/shell/section3/index.ts
autonomous: true
requirements:
  - SHEL-03
must_haves:
  truths:
    - GPS detection and out-and-back detection UI bindings are extracted from main.ts
    - `initializeSection3` delegates completely to `frontend/src/shell/section3/`
  artifacts:
    - path: frontend/src/shell/section3/bindGpsDetection.ts
      provides: Binds GPS lap detection sliders
      min_lines: 20
    - path: frontend/src/shell/section3/bindOutAndBackDetection.ts
      provides: Binds Out-and-back gate sliders
      min_lines: 20
  key_links:
    - from: frontend/src/main.ts
      to: frontend/src/shell/section3/index.ts
      via: imports
---

<objective>
Extract Section 3 lap-selection and GPS-detection shell behavior.
</objective>

<execution_context>
@.pi/gsd/workflows/execute-plan.md
@.pi/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-section-3-and-standard-ve-shell-extraction/03-CONTEXT.md
@.planning/phases/03-section-3-and-standard-ve-shell-extraction/03-RESEARCH.md
@frontend/src/main.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Extract GPS and Out-and-back detection bindings</name>
  <files>frontend/src/shell/section3/bindGpsDetection.ts, frontend/src/shell/section3/bindOutAndBackDetection.ts, frontend/src/shell/section3/index.ts</files>
  <read_first>frontend/src/main.ts</read_first>
  <action>
Create `frontend/src/shell/section3/bindGpsDetection.ts`:
- Export a function `bindGpsDetection` that takes `appState` and `updateAnalyzeButton` callback.
- Move the GPS gate slider and button binding logic from `initializeSection3` into this function.

Create `frontend/src/shell/section3/bindOutAndBackDetection.ts`:
- Export a function `bindOutAndBackDetection` that takes `appState` and `updateAnalyzeButton` callback.
- Move the Out-and-back gate slider and button binding logic from `initializeSection3` into this function.

Update `frontend/src/shell/section3/index.ts` to export these new modules.
  </action>
  <verify>
    <automated>cd frontend && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/shell/section3/bindGpsDetection.ts` exports `bindGpsDetection`
    - `frontend/src/shell/section3/bindOutAndBackDetection.ts` exports `bindOutAndBackDetection`
  </acceptance_criteria>
  <done>GPS and out-and-back bindings extracted.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor initializeSection3 to use the new binders</name>
  <files>frontend/src/main.ts</files>
  <read_first>frontend/src/main.ts</read_first>
  <action>
In `frontend/src/main.ts`:
- Import `bindGpsDetection` and `bindOutAndBackDetection` from `./shell/section3`.
- Replace the inline binding logic for GPS lap detection and Out-and-back detection in `initializeSection3` with calls to these new functions.
- `initializeSection3` should now be a thin orchestrator that calls `renderSection3Template`, `bindLapSelection`, `bindGpsDetection`, `bindOutAndBackDetection`, and the MapVisualization init.
  </action>
  <verify>
    <automated>cd frontend && npm run check && npm run test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/main.ts` calls `bindGpsDetection`
    - `frontend/src/main.ts` calls `bindOutAndBackDetection`
  </acceptance_criteria>
  <done>initializeSection3 is thinner and delegates completely.</done>
</task>
</tasks>

<verification>
- [ ] Verify `npm run check` passes.
- [ ] Verify `npm run build` passes.
</verification>

<success_criteria>
- Section 3 GPS and out-and-back bindings are extracted out of main.ts.
- CI pipeline is green.
</success_criteria>

<output>
After completion, create .planning/phases/03-section-3-and-standard-ve-shell-extraction/03-01-SUMMARY.md
</output>
