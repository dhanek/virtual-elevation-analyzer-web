---
phase: 01-guardrails-and-regression-protection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/testing/ui-shell-regression-contract.md
  - docs/testing/ui-shell-manual-checklist.md
autonomous: true
requirements:
  - STAB-01
user_setup: []
must_haves:
  truths:
    - Maintainer can read a committed regression contract that names the exact UI-shell behaviors Phase 2+ refactors must preserve.
    - Maintainer has a committed manual verification checklist with explicit expected outcomes for file-load navigation, GPS in-place updates, and GPS calibration behavior.
  artifacts:
    - path: docs/testing/ui-shell-regression-contract.md
      provides: Guardrail contract for fragile UI-shell behavior
      min_lines: 40
    - path: docs/testing/ui-shell-manual-checklist.md
      provides: Repeatable browser verification checklist
      min_lines: 30
  key_links:
    - from: docs/testing/ui-shell-regression-contract.md
      to: frontend/src/main.ts
      via: Source-anchor references to the current implementation functions
    - from: docs/testing/ui-shell-manual-checklist.md
      to: docs/testing/ui-shell-regression-contract.md
      via: Checklist sections reference contract sections and expected outcomes
---

<objective>
Capture the regression-sensitive UI-shell contract before larger shell extraction begins.

Purpose: Make the fragile browser behavior explicit so later refactors stop depending on chat memory or tribal knowledge.
Output: A committed regression contract plus a committed manual verification checklist for the highest-risk frontend flows.
</objective>

<execution_context>
@.pi/gsd/workflows/execute-plan.md
@.pi/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-guardrails-and-regression-protection/01-CONTEXT.md
@.planning/phases/01-guardrails-and-regression-protection/01-RESEARCH.md
@.planning/research/SUMMARY.md
@.planning/codebase/CONCERNS.md
@frontend/src/main.ts
@frontend/src/analysis/AirSpeedCalibration.ts
@frontend/src/analysis/MultiSegmentSettings.ts
@frontend/src/modes/analysis/AnalysisModes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the UI-shell regression contract with concrete source anchors</name>
  <files>docs/testing/ui-shell-regression-contract.md</files>
  <read_first>frontend/src/main.ts, frontend/src/analysis/AirSpeedCalibration.ts, frontend/src/analysis/MultiSegmentSettings.ts, frontend/src/modes/analysis/AnalysisModes.ts, .planning/phases/01-guardrails-and-regression-protection/01-CONTEXT.md, .planning/research/SUMMARY.md</read_first>
  <action>Create `docs/testing/ui-shell-regression-contract.md` as the source-of-truth guardrail document for later shell refactors. Add exact sections `## File-load navigation`, `## GPS in-place update behavior`, `## GPS calibration behavior`, and `## CI checkpoint baseline`. In `## File-load navigation`, name `processFitFile`, `processCsvFile`, and `scrollToSection('parametersSection')` as the current anchors for the auto-scroll expectation per D-01. In `## GPS in-place update behavior`, name `showGpsLapVEPlot`, `updateGpsLapVEPlots`, `showOutAndBackVEPlot`, and `updateOutAndBackVEPlots` as the current anchors, and state explicitly that later refactors must not reset the active tab or scroll position per D-02. In `## GPS calibration behavior`, name `calculateAutoAirSpeedCalibrationPercent`, `resolveMultiSegmentSettings`, and `AnalysisModes` as the current shared logic anchors, and state explicitly that GPS lap, GPS gate one-way, and out-and-back modes must keep correct calibration semantics per D-03. In `## CI checkpoint baseline`, copy the current CI parity command chain from `.github/workflows/deploy.yml` exactly. Do not generalize the behavior into vague statements; use the exact mode names and function names so later phases can grep for them.</action>
  <verify>
    <automated>rg "## File-load navigation|## GPS in-place update behavior|## GPS calibration behavior|## CI checkpoint baseline|processFitFile|processCsvFile|scrollToSection\('parametersSection'\)|updateGpsLapVEPlots|updateOutAndBackVEPlots|calculateAutoAirSpeedCalibrationPercent|resolveMultiSegmentSettings" docs/testing/ui-shell-regression-contract.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/testing/ui-shell-regression-contract.md` contains `## File-load navigation`, `## GPS in-place update behavior`, `## GPS calibration behavior`, and `## CI checkpoint baseline`
    - `docs/testing/ui-shell-regression-contract.md` contains `processFitFile`, `processCsvFile`, `scrollToSection('parametersSection')`, `updateGpsLapVEPlots`, and `updateOutAndBackVEPlots`
    - `docs/testing/ui-shell-regression-contract.md` contains `calculateAutoAirSpeedCalibrationPercent` and `resolveMultiSegmentSettings`
    - `docs/testing/ui-shell-regression-contract.md` explicitly states `do not reset the active tab or scroll position`
  </acceptance_criteria>
  <done>The guardrail contract is committed, grep-verifiable, and names the exact code anchors and behaviors later shell extraction must preserve.</done>
</task>

<task type="auto">
  <name>Task 2: Write the manual browser checklist with exact expected outcomes</name>
  <files>docs/testing/ui-shell-manual-checklist.md</files>
  <read_first>docs/testing/ui-shell-regression-contract.md, frontend/src/main.ts, .planning/phases/01-guardrails-and-regression-protection/01-CONTEXT.md</read_first>
  <action>Create `docs/testing/ui-shell-manual-checklist.md` as the repeatable browser checklist for the fragile flows documented in the regression contract. Add exact sections `## Prerequisites`, `## FIT and CSV file-load navigation`, `## GPS lap in-place update checks`, `## Out-and-back in-place update checks`, and `## Calibration checks`. In `## Prerequisites`, require reusing the same local GPS-enabled ride file across runs and note that the operator should start from a clean page load. In the file-load section, require verifying that the Analysis Parameters section becomes visible immediately after a successful local FIT load and after a successful CSV load. In the GPS sections, require switching away from the default VE tab before auto-adjust or slider changes, then verifying the active tab remains unchanged and the scroll position stays near the current VE panel. In the calibration section, require verifying that the displayed calibration value updates without reverting saved CDA/CRR values when the selection is unchanged and that GPS lap, GPS gate one-way, and out-and-back remain aligned with the regression contract. Reference the matching sections in `ui-shell-regression-contract.md` explicitly so the checklist and contract cannot drift apart.</action>
  <verify>
    <automated>rg "## Prerequisites|## FIT and CSV file-load navigation|## GPS lap in-place update checks|## Out-and-back in-place update checks|## Calibration checks|Analysis Parameters section becomes visible|active tab remains unchanged|scroll position stays near the current VE panel|GPS gate one-way" docs/testing/ui-shell-manual-checklist.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/testing/ui-shell-manual-checklist.md` contains `## FIT and CSV file-load navigation`, `## GPS lap in-place update checks`, `## Out-and-back in-place update checks`, and `## Calibration checks`
    - `docs/testing/ui-shell-manual-checklist.md` contains `Analysis Parameters section becomes visible`
    - `docs/testing/ui-shell-manual-checklist.md` contains `active tab remains unchanged` and `scroll position stays near the current VE panel`
    - `docs/testing/ui-shell-manual-checklist.md` contains `GPS gate one-way` and references `ui-shell-regression-contract.md`
  </acceptance_criteria>
  <done>The browser checklist is committed, grep-verifiable, and precise enough that a maintainer can run the fragile checks the same way before and after later shell refactors.</done>
</task>

</tasks>

<verification>
- [ ] `rg "## File-load navigation|## GPS calibration behavior" docs/testing/ui-shell-regression-contract.md` succeeds
- [ ] `rg "active tab remains unchanged|Analysis Parameters section becomes visible" docs/testing/ui-shell-manual-checklist.md` succeeds
- [ ] The checklist references the contract doc explicitly instead of duplicating unanchored behavior descriptions
</verification>

<success_criteria>
- All tasks completed
- Both docs are committed and grep-verifiable
- The fragile browser behaviors are named explicitly, not implied
- Later phases can use these docs as the phase-0 regression contract without re-discovering the same flows
</success_criteria>

<output>
After completion, create `.planning/phases/01-guardrails-and-regression-protection/01-01-SUMMARY.md`
</output>
