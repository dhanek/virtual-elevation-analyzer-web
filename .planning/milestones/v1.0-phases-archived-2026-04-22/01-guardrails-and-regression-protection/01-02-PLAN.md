---
phase: 01-guardrails-and-regression-protection
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - scripts/validate-ui-shell-guardrails.sh
  - docs/testing/ui-shell-manual-checklist.md
  - docs/testing/ui-shell-regression-contract.md
autonomous: true
requirements:
  - STAB-01
  - STAB-02
user_setup: []
must_haves:
  truths:
    - Maintainer can run a single repo script that executes the committed CI parity sequence and then routes them to the manual browser checklist for fragile UI-shell flows.
    - The committed validation path mirrors `.github/workflows/deploy.yml` instead of inventing a weaker local-only contract.
  artifacts:
    - path: scripts/validate-ui-shell-guardrails.sh
      provides: Repeatable guardrail validation entry point
      min_lines: 35
    - path: docs/testing/ui-shell-manual-checklist.md
      provides: Script-linked manual browser verification steps
      min_lines: 35
    - path: docs/testing/ui-shell-regression-contract.md
      provides: Contract doc with explicit CI entry point
      min_lines: 45
  key_links:
    - from: scripts/validate-ui-shell-guardrails.sh
      to: .github/workflows/deploy.yml
      via: Script uses the same backend/frontend command chain as the deploy workflow
    - from: scripts/validate-ui-shell-guardrails.sh
      to: docs/testing/ui-shell-manual-checklist.md
      via: Script prints or references the follow-up manual browser checklist path
---

<objective>
Wire the repeatable regression verification path for the fragile UI shell.

Purpose: Turn the guardrail contract into something maintainers can actually run before and after the later extraction phases.
Output: A repo-level validation script that mirrors CI parity plus updated docs that point maintainers to the exact follow-up browser checks.
</objective>

<execution_context>
@.pi/gsd/workflows/execute-plan.md
@.pi/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-guardrails-and-regression-protection/01-CONTEXT.md
@.planning/phases/01-guardrails-and-regression-protection/01-RESEARCH.md
@.planning/phases/01-guardrails-and-regression-protection/01-01-SUMMARY.md
@.github/workflows/deploy.yml
@package.json
@build.sh
@docs/testing/ui-shell-regression-contract.md
@docs/testing/ui-shell-manual-checklist.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the repo-level guardrail validation script</name>
  <files>scripts/validate-ui-shell-guardrails.sh</files>
  <read_first>.github/workflows/deploy.yml, build.sh, package.json, .planning/phases/01-guardrails-and-regression-protection/01-RESEARCH.md, docs/testing/ui-shell-regression-contract.md, docs/testing/ui-shell-manual-checklist.md</read_first>
  <action>Create `scripts/validate-ui-shell-guardrails.sh` as an executable bash script with `set -euo pipefail`. The script must support `--help` and `--ci-only`. In default mode, run the exact CI parity chain from `.github/workflows/deploy.yml`: `cd backend && cargo test --lib`, `cd backend && wasm-pack build --target web --out-dir ../frontend/pkg`, `cd frontend && npm run check`, `cd frontend && npm run lint`, `cd frontend && npm run test`, and `cd frontend && npm run build`. After the automated commands succeed in default mode, print a follow-up message that points to `docs/testing/ui-shell-manual-checklist.md` and tells the maintainer to run the manual browser checks for file-load navigation, GPS in-place updates, and calibration behavior. In `--ci-only` mode, run the automated command chain only and skip the manual reminder. Do not add `npm install`, `npm ci`, or custom build logic; Phase 1 must mirror the deploy workflow rather than inventing a new contract.</action>
  <verify>
    <automated>bash -n scripts/validate-ui-shell-guardrails.sh && bash scripts/validate-ui-shell-guardrails.sh --help | rg "--ci-only|ui-shell-manual-checklist.md|cargo test --lib|wasm-pack build"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/validate-ui-shell-guardrails.sh` contains `--ci-only` and `ui-shell-manual-checklist.md`
    - `scripts/validate-ui-shell-guardrails.sh` contains `cargo test --lib`, `wasm-pack build --target web --out-dir ../frontend/pkg`, `npm run check`, `npm run lint`, `npm run test`, and `npm run build`
    - `bash -n scripts/validate-ui-shell-guardrails.sh` exits 0
    - `bash scripts/validate-ui-shell-guardrails.sh --help` prints `--ci-only`
  </acceptance_criteria>
  <done>The repo has a single command for the automated guardrail path, and that command mirrors the existing CI parity sequence exactly.</done>
</task>

<task type="auto">
  <name>Task 2: Link the script into the guardrail docs as the single entry point</name>
  <files>docs/testing/ui-shell-manual-checklist.md, docs/testing/ui-shell-regression-contract.md</files>
  <read_first>scripts/validate-ui-shell-guardrails.sh, docs/testing/ui-shell-manual-checklist.md, docs/testing/ui-shell-regression-contract.md, .github/workflows/deploy.yml</read_first>
  <action>Update `docs/testing/ui-shell-manual-checklist.md` and `docs/testing/ui-shell-regression-contract.md` so both docs point maintainers to the new script as the default entry point. Add the exact commands `bash scripts/validate-ui-shell-guardrails.sh` and `bash scripts/validate-ui-shell-guardrails.sh --ci-only` verbatim. In the contract doc, state explicitly that `.github/workflows/deploy.yml` is the source of truth for the automated command chain and that the script mirrors it. In the checklist doc, put the default script command at the top of the workflow so maintainers start with the automated checks before performing browser steps. Do not replace the explicit manual expectations with generic prose; keep the detailed browser checks and merely wire them behind the new command.</action>
  <verify>
    <automated>rg "bash scripts/validate-ui-shell-guardrails.sh|bash scripts/validate-ui-shell-guardrails.sh --ci-only|.github/workflows/deploy.yml" docs/testing/ui-shell-manual-checklist.md docs/testing/ui-shell-regression-contract.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/testing/ui-shell-manual-checklist.md` contains `bash scripts/validate-ui-shell-guardrails.sh`
    - `docs/testing/ui-shell-manual-checklist.md` contains `bash scripts/validate-ui-shell-guardrails.sh --ci-only`
    - `docs/testing/ui-shell-regression-contract.md` contains `.github/workflows/deploy.yml`
    - Both docs retain explicit manual/browser expectations instead of replacing them with generic text
  </acceptance_criteria>
  <done>The regression contract and checklist now point to one repeatable entry point, and the automated path is explicitly tied back to the deploy workflow.</done>
</task>

</tasks>

<verification>
- [ ] `bash -n scripts/validate-ui-shell-guardrails.sh` succeeds
- [ ] `bash scripts/validate-ui-shell-guardrails.sh --help` prints usage including `--ci-only`
- [ ] `rg "bash scripts/validate-ui-shell-guardrails.sh" docs/testing/ui-shell-manual-checklist.md docs/testing/ui-shell-regression-contract.md` succeeds
</verification>

<success_criteria>
- All tasks completed
- A single repo script mirrors the CI parity chain exactly
- The manual checklist is reachable from the script path without relying on chat memory
- Phase 2+ refactors have a clear pre/post-run guardrail command
</success_criteria>

<output>
After completion, create `.planning/phases/01-guardrails-and-regression-protection/01-02-SUMMARY.md`
</output>
