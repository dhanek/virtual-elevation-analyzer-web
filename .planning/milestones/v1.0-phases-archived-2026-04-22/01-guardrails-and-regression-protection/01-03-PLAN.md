---
phase: 01-guardrails-and-regression-protection
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/report-ui-shell-hotspots.sh
  - docs/architecture/frontend-ui-shell-extraction-inventory.md
autonomous: true
requirements:
  - STAB-01
user_setup: []
must_haves:
  truths:
    - Maintainer can regenerate the current `frontend/src/main.ts` hotspot baseline instead of relying on stale line-count anecdotes.
    - Maintainer has a committed extraction inventory that groups current hotspots into the future shell ownership buckets.
  artifacts:
    - path: scripts/report-ui-shell-hotspots.sh
      provides: Regeneratable hotspot baseline report
      min_lines: 25
    - path: docs/architecture/frontend-ui-shell-extraction-inventory.md
      provides: Committed extraction target inventory for later phases
      min_lines: 40
  key_links:
    - from: scripts/report-ui-shell-hotspots.sh
      to: frontend/src/main.ts
      via: Script reports line-count, DOM wiring, and hotspot function metrics from the current shell file
    - from: docs/architecture/frontend-ui-shell-extraction-inventory.md
      to: scripts/report-ui-shell-hotspots.sh
      via: Inventory doc includes the regeneration command and baseline output source
---

<objective>
Create the first durable hotspot inventory and regeneration command for the remaining frontend shell.

Purpose: Give later extraction phases a stable, committed baseline of what `frontend/src/main.ts` still owns and where the future shell boundaries should land.
Output: A hotspot-report script plus a committed extraction inventory document.
</objective>

<execution_context>
@.pi/gsd/workflows/execute-plan.md
@.pi/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-guardrails-and-regression-protection/01-CONTEXT.md
@.planning/phases/01-guardrails-and-regression-protection/01-RESEARCH.md
@.planning/codebase/CONCERNS.md
@frontend/src/main.ts
@frontend/src/components/MapVisualization.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a regeneratable hotspot report script for the current shell</name>
  <files>scripts/report-ui-shell-hotspots.sh</files>
  <read_first>frontend/src/main.ts, .planning/codebase/CONCERNS.md, build.sh</read_first>
  <action>Create `scripts/report-ui-shell-hotspots.sh` as an executable bash script with `set -euo pipefail`. The script must report the current `frontend/src/main.ts` line count and the counts for `document.getElementById(`, `addEventListener(`, `innerHTML = \``, `style=\"`, and `: any`. It must also print line anchors for the named hotspot functions `initializeSection3`, `handleAnalyze`, `showVirtualElevationAnalysisInline`, `showGpsLapVEPlot`, `showOutAndBackVEPlot`, `updateGpsLapVEPlots`, `updateOutAndBackVEPlots`, `setupVESliders`, and `calculateAutoRho` using `rg -n`. Keep the script dependency-free: use only repo-available shell tools like `wc`, `rg`, `awk`, and `printf`. Do not write a generic repo analyzer; make the script explicitly about the UI-shell stabilization target and the known hotspots from `.planning/codebase/CONCERNS.md`.</action>
  <verify>
    <automated>bash -n scripts/report-ui-shell-hotspots.sh && bash scripts/report-ui-shell-hotspots.sh | rg "frontend/src/main.ts|document.getElementById|addEventListener|initializeSection3|setupVESliders|updateOutAndBackVEPlots"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/report-ui-shell-hotspots.sh` contains `frontend/src/main.ts`, `document.getElementById`, `addEventListener`, `innerHTML = \``, `style=\"`, and `: any`
    - `scripts/report-ui-shell-hotspots.sh` contains `initializeSection3`, `handleAnalyze`, `showVirtualElevationAnalysisInline`, `showGpsLapVEPlot`, `showOutAndBackVEPlot`, `updateGpsLapVEPlots`, `updateOutAndBackVEPlots`, `setupVESliders`, and `calculateAutoRho`
    - `bash -n scripts/report-ui-shell-hotspots.sh` exits 0
    - `bash scripts/report-ui-shell-hotspots.sh` prints `frontend/src/main.ts` and at least one hotspot function name
  </acceptance_criteria>
  <done>The repo has a runnable, dependency-free script that re-measures the key `main.ts` shell hotspots whenever later phases need a fresh baseline.</done>
</task>

<task type="auto">
  <name>Task 2: Freeze the current extraction inventory and planned ownership buckets</name>
  <files>docs/architecture/frontend-ui-shell-extraction-inventory.md</files>
  <read_first>scripts/report-ui-shell-hotspots.sh, .planning/codebase/CONCERNS.md, .planning/phases/01-guardrails-and-regression-protection/01-RESEARCH.md, frontend/src/main.ts, frontend/src/components/MapVisualization.ts</read_first>
  <action>Run `bash scripts/report-ui-shell-hotspots.sh` and use its output plus the existing concerns doc to create `docs/architecture/frontend-ui-shell-extraction-inventory.md`. Include sections `## Current baseline`, `## Priority ownership buckets`, `## Phase mapping`, and `## Secondary hotspot guidance`. In `## Current baseline`, record the measured baseline and the command `bash scripts/report-ui-shell-hotspots.sh` as the regeneration command. In `## Priority ownership buckets`, group later extraction targets into `Section 3 shell`, `Standard VE shell`, `GPS-lap shell`, `Out-and-back shell`, and `Shared DOM/event/template helpers`, and assign the known hotspot functions to those buckets explicitly. In `## Phase mapping`, map the buckets to roadmap Phases 2-4. In `## Secondary hotspot guidance`, explain that `frontend/src/components/MapVisualization.ts` remains secondary and should only be touched when it clearly supports the main shell extraction path. Do not leave the inventory at the level of “main.ts is big”; it must name exact functions and future owners.</action>
  <verify>
    <automated>rg "## Current baseline|## Priority ownership buckets|## Phase mapping|## Secondary hotspot guidance|Section 3 shell|Standard VE shell|GPS-lap shell|Out-and-back shell|Shared DOM/event/template helpers|bash scripts/report-ui-shell-hotspots.sh|MapVisualization.ts" docs/architecture/frontend-ui-shell-extraction-inventory.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/architecture/frontend-ui-shell-extraction-inventory.md` contains `## Current baseline`, `## Priority ownership buckets`, `## Phase mapping`, and `## Secondary hotspot guidance`
    - `docs/architecture/frontend-ui-shell-extraction-inventory.md` contains `Section 3 shell`, `Standard VE shell`, `GPS-lap shell`, `Out-and-back shell`, and `Shared DOM/event/template helpers`
    - `docs/architecture/frontend-ui-shell-extraction-inventory.md` contains `bash scripts/report-ui-shell-hotspots.sh`
    - `docs/architecture/frontend-ui-shell-extraction-inventory.md` contains `MapVisualization.ts` and states it is secondary
  </acceptance_criteria>
  <done>The extraction inventory is committed, reproducible, and explicit enough that later phases know which hotspot functions belong to which future shell modules.</done>
</task>

</tasks>

<verification>
- [ ] `bash scripts/report-ui-shell-hotspots.sh` runs successfully
- [ ] `rg "Section 3 shell|GPS-lap shell|MapVisualization.ts" docs/architecture/frontend-ui-shell-extraction-inventory.md` succeeds
- [ ] The inventory doc includes a regeneration command and future ownership buckets
</verification>

<success_criteria>
- All tasks completed
- The hotspot baseline is regeneratable from a committed script
- The inventory doc names exact hotspot functions and planned ownership buckets
- Later phases can use the inventory directly instead of re-measuring from scratch
</success_criteria>

<output>
After completion, create `.planning/phases/01-guardrails-and-regression-protection/01-03-SUMMARY.md`
</output>
