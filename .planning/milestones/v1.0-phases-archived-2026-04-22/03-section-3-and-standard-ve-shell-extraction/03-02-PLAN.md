---
phase: 03-section-3-and-standard-ve-shell-extraction
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - frontend/src/main.ts
  - frontend/src/shell/ve/autoRho.ts
  - frontend/src/shell/ve/bindStandardSliders.ts
  - frontend/src/shell/ve/renderStandardVe.ts
  - frontend/src/shell/ve/index.ts
autonomous: true
requirements:
  - SHEL-04
must_haves:
  truths:
    - `setupVESliders`, `showVirtualElevationAnalysisInline`, and `calculateAutoRho` are extracted from main.ts
    - `handleAnalyze` calls the extracted standard VE render delegate
  artifacts:
    - path: frontend/src/shell/ve/autoRho.ts
      provides: Auto rho calculation
      min_lines: 50
    - path: frontend/src/shell/ve/bindStandardSliders.ts
      provides: Setup VE sliders
      min_lines: 50
    - path: frontend/src/shell/ve/renderStandardVe.ts
      provides: Render standard VE
      min_lines: 50
  key_links:
    - from: frontend/src/main.ts
      to: frontend/src/shell/ve/renderStandardVe.ts
      via: imports
---

<objective>
Extract standard VE panel render, bind, and update shell behavior.
</objective>

<execution_context>
@.pi/gsd/workflows/execute-plan.md
@.pi/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-section-3-and-standard-ve-shell-extraction/03-CONTEXT.md
@.planning/phases/03-section-3-and-standard-ve-shell-extraction/03-RESEARCH.md
@frontend/src/main.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Extract calculateAutoRho</name>
  <files>frontend/src/shell/ve/autoRho.ts, frontend/src/shell/ve/index.ts, frontend/src/main.ts</files>
  <read_first>frontend/src/main.ts</read_first>
  <action>
Create `frontend/src/shell/ve/autoRho.ts`:
- Export the `calculateAutoRho` function.
- It will need `appState` (or the necessary inputs like `currentFitData`, `currentParameters`, `filteredVEData`).
- Update imports in this new file to satisfy the logic.

In `frontend/src/shell/ve/index.ts`:
- Export `autoRho.ts`.

In `frontend/src/main.ts`:
- Import `calculateAutoRho` from `./shell/ve`.
- Replace the inline implementation with this import.
  </action>
  <verify>
    <automated>cd frontend && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/shell/ve/autoRho.ts` exports `calculateAutoRho`
  </acceptance_criteria>
  <done>calculateAutoRho extracted.</done>
</task>

<task type="auto">
  <name>Task 2: Extract setupVESliders</name>
  <files>frontend/src/shell/ve/bindStandardSliders.ts, frontend/src/shell/ve/index.ts, frontend/src/main.ts</files>
  <read_first>frontend/src/main.ts</read_first>
  <action>
Create `frontend/src/shell/ve/bindStandardSliders.ts`:
- Export the `setupVESliders` function.
- Include `updateVEPlotsWithWindSource` and `updateVEPlots` which are closely related to slider updates.
- Pass `appState` or other dependencies as needed.

In `frontend/src/main.ts`:
- Import these functions from `./shell/ve`.
  </action>
  <verify>
    <automated>cd frontend && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/shell/ve/bindStandardSliders.ts` exports `setupVESliders`
  </acceptance_criteria>
  <done>setupVESliders extracted.</done>
</task>

<task type="auto">
  <name>Task 3: Extract showVirtualElevationAnalysisInline</name>
  <files>frontend/src/shell/ve/renderStandardVe.ts, frontend/src/shell/ve/index.ts, frontend/src/main.ts</files>
  <read_first>frontend/src/main.ts</read_first>
  <action>
Create `frontend/src/shell/ve/renderStandardVe.ts`:
- Move `showVirtualElevationAnalysisInline` and `initializeVEAnalysis` from `main.ts` into this file.
- Handle any imports required (e.g. plot builders, `setupTabSwitching`).

In `frontend/src/main.ts`:
- Import `showVirtualElevationAnalysisInline` from `./shell/ve`.
  </action>
  <verify>
    <automated>cd frontend && npm run check && npm run test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/shell/ve/renderStandardVe.ts` exports `showVirtualElevationAnalysisInline`
  </acceptance_criteria>
  <done>showVirtualElevationAnalysisInline extracted.</done>
</task>
</tasks>

<verification>
- [ ] Verify `npm run check` passes.
- [ ] Verify `npm run build` passes.
</verification>

<success_criteria>
- Standard VE shell logic extracted out of main.ts.
- CI pipeline is green.
</success_criteria>

<output>
After completion, create .planning/phases/03-section-3-and-standard-ve-shell-extraction/03-02-SUMMARY.md
</output>
