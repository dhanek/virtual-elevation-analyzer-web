---
phase: 04-gps-and-out-and-back-shell-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/shell/gpsLap/types.ts
  - frontend/src/shell/gpsLap/gpsLapPlots.ts
  - frontend/src/shell/gpsLap/renderGpsLap.ts
  - frontend/src/shell/gpsLap/updateGpsLap.ts
  - frontend/src/shell/gpsLap/gpsLapScreenshot.ts
  - frontend/src/shell/gpsLap/index.ts
  - frontend/src/shell/multiSegment/shared.ts
  - frontend/src/analysis/MultiSegmentSettings.ts
  - frontend/src/main.ts
autonomous: true
requirements: [SHEL-05, BEHV-03, BEHV-04]

must_haves:
  truths:
    - "GPS-lap shell render, plot, update, and screenshot functions live in shell/gpsLap/ instead of main.ts"
    - "Persistence helpers (resolveMultiSegmentAnalysisParams, saveCurrentMultiSegmentSettings, saveMapTrimSettings, buildAutoCalibrationSegmentsFromRanges) live in analysis/MultiSegmentSettings.ts"
    - "Shared multi-segment color/interpolation helpers live in shell/multiSegment/shared.ts"
    - "GPS-lap analysis still invokes correctly via handleAnalyze -> createModeRenderCallbacks -> gpsLap callback"
  artifacts:
    - path: "frontend/src/shell/gpsLap/renderGpsLap.ts"
      provides: "showGpsLapVEAnalysis, showGpsLapVEPlot, setupGpsLapSliderHandlers, getGpsLapNumberForRange"
      min_lines: 200
    - path: "frontend/src/shell/gpsLap/gpsLapPlots.ts"
      provides: "renderGpsLapVEPlots, renderGpsLapWindPlot, renderGpsLapPowerPlot, renderGpsLapVdPlot, calculateGpsLapStats, calculateMeanElevationProfile"
      min_lines: 150
    - path: "frontend/src/shell/gpsLap/updateGpsLap.ts"
      provides: "updateGpsLapVEPlots, recalculateGpsLapVE"
      min_lines: 100
    - path: "frontend/src/shell/gpsLap/gpsLapScreenshot.ts"
      provides: "saveGpsLapScreenshot"
      min_lines: 10
    - path: "frontend/src/shell/gpsLap/types.ts"
      provides: "LapVEProfile interface"
      contains: "interface LapVEProfile"
    - path: "frontend/src/shell/gpsLap/index.ts"
      provides: "barrel re-exports"
    - path: "frontend/src/shell/multiSegment/shared.ts"
      provides: "getMultiSegmentColor, MULTI_SEGMENT_COLORS, interpolateElevation"
      contains: "export function getMultiSegmentColor"
    - path: "frontend/src/analysis/MultiSegmentSettings.ts"
      provides: "resolveMultiSegmentAnalysisParams, saveCurrentMultiSegmentSettings, saveMapTrimSettings, buildAutoCalibrationSegmentsFromRanges"
      min_lines: 150
  key_links:
    - from: "frontend/src/main.ts"
      to: "frontend/src/shell/gpsLap/renderGpsLap.ts"
      via: "import showGpsLapVEAnalysis in createModeRenderCallbacks gpsLap callback"
      pattern: "import.*showGpsLapVEAnalysis.*from.*shell/gpsLap"
    - from: "frontend/src/shell/gpsLap/renderGpsLap.ts"
      to: "frontend/src/analysis/MultiSegmentSettings.ts"
      via: "import resolveMultiSegmentAnalysisParams, saveCurrentMultiSegmentSettings"
      pattern: "import.*from.*analysis/MultiSegmentSettings"
    - from: "frontend/src/shell/gpsLap/gpsLapPlots.ts"
      to: "frontend/src/shell/multiSegment/shared.ts"
      via: "import getMultiSegmentColor"
      pattern: "import.*getMultiSegmentColor.*from.*multiSegment/shared"
---

<objective>
Extract GPS-lap shell behavior (~1300 lines) from main.ts into `shell/gpsLap/`, consolidate persistence helpers into `analysis/MultiSegmentSettings.ts`, and create `shell/multiSegment/shared.ts` for shared color/interpolation helpers. This is the first extraction step; out-and-back follows in plan 04-02.

Purpose: Achieve SHEL-05 (GPS-lap shell isolated from unrelated modes) and establish the shared infrastructure (MultiSegmentSettings persistence, multiSegment/shared helpers) that plan 04-02 also needs.
Output: 8 new files under shell/gpsLap/ and shell/multiSegment/, expanded MultiSegmentSettings.ts, reduced main.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-gps-and-out-and-back-shell-extraction/04-CONTEXT.md
@.planning/phases/04-gps-and-out-and-back-shell-extraction/04-RESEARCH.md
@.planning/phases/04-gps-and-out-and-back-shell-extraction/04-PATTERNS.md

# Phase 3 summary for extraction pattern reference
@.planning/phases/03-section-3-and-standard-ve-shell-extraction/03-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From frontend/src/shell/analysis/types.ts:
```typescript
export interface ShellServices {
    appState: AppState;
    showLoading: (message: string) => void;
    hideLoading: () => void;
    showError: (message: string) => void;
}

export interface ShellAnalysisContext extends ShellServices {
    waitForPlotly: () => Promise<void>;
}
```

From frontend/src/modes/analysis/types.ts:
```typescript
export interface ModeRenderCallbacks {
    standard(args: StandardRenderArgs): Promise<void> | void;
    gpsLap(args: {
        lapIndexRanges: LapIndexRange[];
        fitData: ActivityDataLike;
        params: AnalysisParameters;
        defaultAirSpeedOffset: number;
    }): Promise<void> | void;
    outAndBack(args: {
        sections: OutAndBackSection[];
        fitData: ActivityDataLike;
        params: AnalysisParameters;
        defaultAirSpeedOffset: number;
    }): Promise<void> | void;
}
```

From frontend/src/shell/ve/index.ts (barrel pattern):
```typescript
export * from './autoRho';
export * from './bindStandardSliders';
export * from './renderStandardVe';
```

From frontend/src/analysis/MultiSegmentSettings.ts (existing, 42 lines):
```typescript
export interface ResolvedMultiSegmentSettings {
    params: AnalysisParameters
    airSpeedCalibrationPercent: number
}
export function resolveMultiSegmentSettings(input: ResolveMultiSegmentSettingsInput): ResolvedMultiSegmentSettings;
export function sameSelection(left: number[], right: number[]): boolean;
```

From frontend/src/main.ts line 2200-2201 (current GPS-lap wiring):
```typescript
gpsLap: ({ lapIndexRanges, fitData, params, defaultAirSpeedOffset }) =>
    showGpsLapVEAnalysis(lapIndexRanges, fitData, params, defaultAirSpeedOffset),
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared infrastructure (multiSegment/shared.ts, MultiSegmentSettings.ts consolidation, gpsLap/types.ts)</name>
  <files>
    frontend/src/shell/multiSegment/shared.ts
    frontend/src/analysis/MultiSegmentSettings.ts
    frontend/src/shell/gpsLap/types.ts
  </files>
  <read_first>
    frontend/src/analysis/MultiSegmentSettings.ts
    frontend/src/main.ts (lines 80-118 for buildAutoCalibrationSegmentsFromRanges, lines 2226-2245 for MULTI_SEGMENT_COLORS and LapVEProfile, lines 2971-2973 for getMultiSegmentColor, lines 3240-3312 for resolveMultiSegmentAnalysisParams/saveCurrentMultiSegmentSettings/saveMapTrimSettings, lines 3830-3842 for interpolateElevation)
    frontend/src/shell/analysis/types.ts
    frontend/src/analysis/AirSpeedCalibration.ts (for calculateAutoAirSpeedCalibrationPercent import)
    frontend/src/analysis/ActivityArrayCache.ts (for getNormalizedActivityArrays import)
    frontend/src/analysis/WindSourceResolver.ts (for resolveWindSeries import)
    frontend/src/analysis/SegmentExtractor.ts (for extractSegmentData import)
    frontend/src/state/AppState.ts (for AppState type)
    frontend/src/utils/ParameterStorage.ts (for ParameterStorage type)
  </read_first>
  <action>
**1. Create `frontend/src/shell/multiSegment/shared.ts`** (per D-02):

Verbatim lift from main.ts. Export:
- `MULTI_SEGMENT_COLORS` array (main.ts lines 2226-2235) as `export const`
- `getMultiSegmentColor(index: number): string` (main.ts lines 2971-2973) returning `MULTI_SEGMENT_COLORS[index % MULTI_SEGMENT_COLORS.length]`
- `interpolateElevation(elevation: number[], distance: number[], targetDistance: number): number` (main.ts lines 3830-3842) -- verbatim pure function

**2. Create `frontend/src/shell/gpsLap/types.ts`**:

Export the `LapVEProfile` interface (main.ts lines 2237-2245):
```typescript
import type { SegmentSupplementarySeries } from '../../analysis/SegmentSupplementarySeries';

export interface LapVEProfile {
    lapNumber: number;
    distances: number[];
    virtualElevation: number[];
    actualElevation: number[];
    supplementarySeries: SegmentSupplementarySeries;
    duration: number;
    totalDistance: number;
}
```

**3. Consolidate persistence helpers into `frontend/src/analysis/MultiSegmentSettings.ts`** (per D-06):

Add these 4 functions below the existing `sameSelection` function. Each must take explicit `appState: AppState` and `parameterStorage: ParameterStorage` parameters instead of closure captures:

- `resolveMultiSegmentAnalysisParams(appState, parameterStorage, analyzedItems, params, reuseCurrentSettings)` -- verbatim from main.ts lines 3240-3259. Uses `appState.currentFileHash`, `appState.currentAnalyzedLaps`, `appState.airSpeedCalibrationPercent`, and `parameterStorage.getLapSettings()`. Returns `Promise<AnalysisParameters>`.

- `saveCurrentMultiSegmentSettings(appState, parameterStorage, analyzedItems?)` -- verbatim from main.ts lines 3261-3290. Uses `appState.currentFileHash`, `appState.selectedFile`, `appState.currentAnalyzedLaps`, `appState.airSpeedCalibrationPercent`, `appState.currentParameters`. Calls `parameterStorage.saveLapSettings()`.

- `saveMapTrimSettings(appState, parameterStorage)` -- verbatim from main.ts lines 3293-3312. Uses `appState.currentFileHash`, `appState.selectedFile`, `appState.presetTrimStart`, `appState.presetTrimEnd`, `appState.selectedLaps`. Calls `parameterStorage.saveMapTrimSettings()`.

- `buildAutoCalibrationSegmentsFromRanges(appState, ranges, getNormalizedActivityArraysFn, resolveWindSeriesFn, extractSegmentDataFn)` -- from main.ts lines 84-118. Takes the analysis helper functions as parameters to avoid importing them directly (keeps MultiSegmentSettings focused on settings). Returns the calibration segments array.

Add required imports at the top: `AppState`, `ParameterStorage`, `AnalysisParameters`, `DEFAULT_AIR_SPEED_CALIBRATION_PERCENT`. Keep existing imports. Export all 4 new functions.

Per D-07: both shell/gpsLap/ and shell/outAndBack/ will import these helpers from this file.
  </action>
  <verify>
    <automated>cd /Users/hannes/Documents/git/virtual-elevation-analyzer-web/frontend && npm run check && npm run test</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/shell/multiSegment/shared.ts contains `export const MULTI_SEGMENT_COLORS`
    - frontend/src/shell/multiSegment/shared.ts contains `export function getMultiSegmentColor`
    - frontend/src/shell/multiSegment/shared.ts contains `export function interpolateElevation`
    - frontend/src/shell/gpsLap/types.ts contains `export interface LapVEProfile`
    - frontend/src/analysis/MultiSegmentSettings.ts contains `export async function resolveMultiSegmentAnalysisParams`
    - frontend/src/analysis/MultiSegmentSettings.ts contains `export async function saveCurrentMultiSegmentSettings`
    - frontend/src/analysis/MultiSegmentSettings.ts contains `export async function saveMapTrimSettings`
    - frontend/src/analysis/MultiSegmentSettings.ts contains `export function buildAutoCalibrationSegmentsFromRanges`
    - npm run check exits 0
    - npm run test exits 0
  </acceptance_criteria>
  <done>Shared infrastructure files exist and compile. MultiSegmentSettings.ts contains all 4 consolidated persistence helpers with explicit parameters. No functionality removed from main.ts yet (that happens in Task 2).</done>
</task>

<task type="auto">
  <name>Task 2: Extract GPS-lap shell functions from main.ts into shell/gpsLap/ and rewire</name>
  <files>
    frontend/src/shell/gpsLap/renderGpsLap.ts
    frontend/src/shell/gpsLap/gpsLapPlots.ts
    frontend/src/shell/gpsLap/updateGpsLap.ts
    frontend/src/shell/gpsLap/gpsLapScreenshot.ts
    frontend/src/shell/gpsLap/index.ts
    frontend/src/main.ts
  </files>
  <read_first>
    frontend/src/main.ts (lines 2224-3515 for all GPS-lap functions, lines 80-118 for buildAutoCalibrationSegmentsFromRanges, lines 2200-2204 for createModeRenderCallbacks gpsLap wiring)
    frontend/src/shell/gpsLap/types.ts (just created in Task 1)
    frontend/src/shell/multiSegment/shared.ts (just created in Task 1)
    frontend/src/analysis/MultiSegmentSettings.ts (just expanded in Task 1)
    frontend/src/shell/ve/renderStandardVe.ts (analog: DI pattern for function signatures)
    frontend/src/shell/ve/bindStandardSliders.ts (analog: update/slider handler pattern)
    frontend/src/shell/dom/tabs.ts (setupTabSwitching import)
    frontend/src/shell/dom/windSource.ts (getSelectedWindSource, bindWindSourceRadios imports)
    frontend/src/shell/dom/actionFooter.ts (bindActionFooter import)
    frontend/src/shell/analysis/storageHandlers.ts (handleStoreResult, handleExportAllResults imports)
    frontend/src/shell/analysis/types.ts (ShellServices, ShellAnalysisContext)
  </read_first>
  <action>
**Create 4 shell module files by verbatim lift from main.ts, converting closure captures to explicit parameters per the ShellServices DI pattern (D-08, D-10):**

**1. `frontend/src/shell/gpsLap/gpsLapPlots.ts`** -- verbatim lift of:
- `calculateMeanElevationProfile` (main.ts lines 2422-2490) -- pure function, no DI needed
- `calculateGpsLapStats` (main.ts lines 2756-2854) -- pure function, no DI needed
- `renderGpsLapVEPlots` (main.ts lines 3044-3228) -- receives `lapProfiles: LapVEProfile[]`, `appState`, `waitForPlotly` as params. Uses `getMultiSegmentColor` from `../multiSegment/shared`
- `renderGpsLapWindPlot` (main.ts lines 2978-2995) -- receives `lapProfiles`, uses `buildMultiSegmentWindFigure` from `../../plots/MultiSegmentPlotBuilders`, `getMultiSegmentColor` from `../multiSegment/shared`
- `renderGpsLapPowerPlot` (main.ts lines 3000-3017) -- same pattern as wind
- `renderGpsLapVdPlot` (main.ts lines 3022-3039) -- same pattern as vd

Export all 6 functions.

**2. `frontend/src/shell/gpsLap/renderGpsLap.ts`** -- verbatim lift of:
- `showGpsLapVEAnalysis` (main.ts lines 2250-2417) -- add explicit params: `services: ShellServices`, `parameterStorage: ParameterStorage`, `resultsStorage: ResultsStorage`. Replace closure-captured `appState` with `services.appState`. Replace `showLoading`/`hideLoading`/`showError` with `services.showLoading`/etc. Import `resolveMultiSegmentAnalysisParams` from `../../analysis/MultiSegmentSettings`. Import `getNormalizedActivityArrays`, `resolveWindSeries`, `createVeCalculator`, `buildSegmentSupplementarySeries`, `extractSegmentData` from their existing locations.
- `showGpsLapVEPlot` (main.ts lines 2495-2751) -- add explicit params: `appState`, `parameterStorage`, `resultsStorage`, `waitForPlotly`. Replace closure captures. Import `setupTabSwitching` from `../dom/tabs`, `bindWindSourceRadios`/`getSelectedWindSource` from `../dom/windSource`, `bindActionFooter` from `../dom/actionFooter`, `calculateAutoAirSpeedCalibrationPercent` from `../../analysis/AirSpeedCalibration`, `buildAutoCalibrationSegmentsFromRanges`/`saveCurrentMultiSegmentSettings` from `../../analysis/MultiSegmentSettings`, `handleStoreResult`/`handleExportAllResults` from `../analysis/storageHandlers`.
- `setupGpsLapSliderHandlers` (main.ts lines 2859-2897) -- receives `appState`, `parameterStorage`, `updateFn` callback
- `getGpsLapNumberForRange` (main.ts lines 3233-3238) -- receives `appState` explicitly

Export all 4 functions. The top-level export is `showGpsLapVEAnalysis`.

**3. `frontend/src/shell/gpsLap/updateGpsLap.ts`** -- verbatim lift of (per D-08, D-09):
- `updateGpsLapVEPlots` (main.ts lines 3318-3514) -- VERBATIM. Add explicit params: `appState`, `parameterStorage`, `waitForPlotly`. Keep the exact tab/scroll save-restore code. Do NOT abstract or DRY with out-and-back update. Import `getNormalizedActivityArrays`, `resolveWindSeries`, `extractSegmentData`, `createVeCalculator` from their existing locations. Import `renderGpsLapVEPlots` from `./gpsLapPlots`. Import `setupTabSwitching` from `../dom/tabs`.
- `recalculateGpsLapVE` (main.ts lines 2902-2948) -- add `appState`, `services`, `parameterStorage`, `resultsStorage` params. Calls `showGpsLapVEAnalysis` from `./renderGpsLap`.

Export both functions.

**4. `frontend/src/shell/gpsLap/gpsLapScreenshot.ts`** -- verbatim lift of:
- `saveGpsLapScreenshot` (main.ts lines 2953-2969) -- receives `waitForPlotly` as param (from ShellAnalysisContext). Export.

**5. `frontend/src/shell/gpsLap/index.ts`** -- barrel exports:
```typescript
export * from './renderGpsLap';
export * from './gpsLapPlots';
export * from './updateGpsLap';
export * from './gpsLapScreenshot';
export type * from './types';
```

**6. Update `frontend/src/main.ts`:**
- Remove ALL GPS-lap functions from main.ts: lines 2224-3515 (MULTI_SEGMENT_COLORS, LapVEProfile, showGpsLapVEAnalysis, calculateMeanElevationProfile, showGpsLapVEPlot, calculateGpsLapStats, setupGpsLapSliderHandlers, recalculateGpsLapVE, saveGpsLapScreenshot, getMultiSegmentColor, renderGpsLapWindPlot, renderGpsLapPowerPlot, renderGpsLapVdPlot, renderGpsLapVEPlots, getGpsLapNumberForRange, resolveMultiSegmentAnalysisParams, saveCurrentMultiSegmentSettings, saveMapTrimSettings, updateGpsLapVEPlots).
- Remove `buildAutoCalibrationSegmentsFromRanges` from lines 84-118 (moved to MultiSegmentSettings.ts).
- Add import: `import { showGpsLapVEAnalysis } from './shell/gpsLap'`
- Update the `gpsLap` callback in `createModeRenderCallbacks` (around line 2200) to pass the required DI params:
  ```typescript
  gpsLap: ({ lapIndexRanges, fitData, params, defaultAirSpeedOffset }) =>
      showGpsLapVEAnalysis(
          { appState, showLoading, hideLoading, showError },
          parameterStorage,
          resultsStorage,
          lapIndexRanges, fitData, params, defaultAirSpeedOffset
      ),
  ```
- Remove now-unused imports that were only needed by the deleted GPS-lap code (check each import after deletion).
- Keep the out-and-back functions in main.ts (plan 04-02 will extract those).

IMPORTANT: The out-and-back code in main.ts still references `MULTI_SEGMENT_COLORS`, `getMultiSegmentColor`, `interpolateElevation`, `resolveMultiSegmentAnalysisParams`, `saveCurrentMultiSegmentSettings`, `saveMapTrimSettings`, and `buildAutoCalibrationSegmentsFromRanges`. After removing these from main.ts, add imports from their new locations (`../shell/multiSegment/shared`, `../analysis/MultiSegmentSettings`) so the out-and-back code still compiles.
  </action>
  <verify>
    <automated>cd /Users/hannes/Documents/git/virtual-elevation-analyzer-web/frontend && npm run check && npm run lint && npm run test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/shell/gpsLap/renderGpsLap.ts contains `export async function showGpsLapVEAnalysis`
    - frontend/src/shell/gpsLap/renderGpsLap.ts contains `export function showGpsLapVEPlot`
    - frontend/src/shell/gpsLap/renderGpsLap.ts contains `export function setupGpsLapSliderHandlers`
    - frontend/src/shell/gpsLap/gpsLapPlots.ts contains `export function renderGpsLapVEPlots`
    - frontend/src/shell/gpsLap/gpsLapPlots.ts contains `export function calculateMeanElevationProfile`
    - frontend/src/shell/gpsLap/gpsLapPlots.ts contains `export function calculateGpsLapStats`
    - frontend/src/shell/gpsLap/gpsLapPlots.ts contains `export function renderGpsLapWindPlot`
    - frontend/src/shell/gpsLap/updateGpsLap.ts contains `export async function updateGpsLapVEPlots`
    - frontend/src/shell/gpsLap/updateGpsLap.ts contains `export async function recalculateGpsLapVE`
    - frontend/src/shell/gpsLap/gpsLapScreenshot.ts contains `export async function saveGpsLapScreenshot`
    - frontend/src/shell/gpsLap/index.ts contains `export * from './renderGpsLap'`
    - frontend/src/main.ts contains `import.*showGpsLapVEAnalysis.*from.*shell/gpsLap`
    - frontend/src/main.ts does NOT contain `function showGpsLapVEAnalysis`
    - frontend/src/main.ts does NOT contain `function updateGpsLapVEPlots`
    - frontend/src/main.ts does NOT contain `function renderGpsLapVEPlots`
    - frontend/src/main.ts does NOT contain `const MULTI_SEGMENT_COLORS`
    - frontend/src/main.ts does NOT contain `function buildAutoCalibrationSegmentsFromRanges`
    - npm run check exits 0
    - npm run lint exits 0
    - npm run test exits 0
    - npm run build exits 0
  </acceptance_criteria>
  <done>All GPS-lap shell functions extracted to shell/gpsLap/. main.ts no longer contains GPS-lap shell code. The gpsLap callback in createModeRenderCallbacks points to the new shell module. Persistence helpers consolidated in MultiSegmentSettings.ts. Out-and-back code in main.ts still compiles with updated imports. Full CI passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. This is a pure structural refactoring of client-side code with no network calls, authentication, or data persistence changes beyond IndexedDB (existing).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | MultiSegmentSettings.ts persistence | accept | Settings stored in IndexedDB (client-only, same origin). No server-side persistence. Existing threat surface unchanged by extraction. |
</threat_model>

<verification>
1. `cd frontend && npm run check` passes (TypeScript compiles)
2. `cd frontend && npm run lint` passes (no lint errors)
3. `cd frontend && npm run test` passes (all unit tests)
4. `cd frontend && npm run build` passes (Vite production build)
5. `bash scripts/validate-ui-shell-guardrails.sh` passes
6. `grep -r "function showGpsLapVEAnalysis" frontend/src/main.ts` returns NO matches
7. `grep -r "showGpsLapVEAnalysis" frontend/src/shell/gpsLap/renderGpsLap.ts` returns matches
</verification>

<success_criteria>
GPS-lap shell code (~1300 lines) fully extracted from main.ts into shell/gpsLap/ modules. Persistence helpers consolidated into analysis/MultiSegmentSettings.ts. Shared multi-segment helpers in shell/multiSegment/shared.ts. main.ts delegates to new modules via imports. All CI checks pass.
</success_criteria>

<output>
After completion, create `.planning/phases/04-gps-and-out-and-back-shell-extraction/04-01-SUMMARY.md`
</output>
