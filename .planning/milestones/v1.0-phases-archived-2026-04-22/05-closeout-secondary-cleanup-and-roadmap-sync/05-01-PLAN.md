---
phase: 05-closeout-secondary-cleanup-and-roadmap-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/main.ts
  - frontend/src/shell/app/initializeApplication.ts
  - frontend/src/shell/analysis/analyzeOrchestrator.ts
  - frontend/src/shell/fileLoad/fileLoadOrchestration.ts
  - frontend/src/shell/section3/section3Orchestration.ts
  - frontend/src/shell/dem/demHandlers.ts
  - frontend/src/components/MapVisualization.ts
autonomous: false
requirements: [CLOS-01]

must_haves:
  truths:
    - "frontend/src/main.ts functions as a composition root (imports + dependency wiring + bootstrap dispatch), not a behavior hotspot"
    - "All residual Phase 5 buckets from D-02/D-03 execute through extracted shell modules rather than local main.ts functions"
    - "Residual helper/status/math lifts are folded into the orchestration extraction surface (no standalone new helper-module fan-out for this plan)"
    - "Regression-sensitive behaviors remain intact: BEHV-01 auto-scroll, BEHV-02 standard VE output, BEHV-03 tab/scroll retention, BEHV-04 GPS calibration correctness"
    - "Any MapVisualization.ts touch is seam-forced, minimal, and behavior-neutral"
  artifacts:
    - path: "frontend/src/shell/fileLoad/fileLoadOrchestration.ts"
      provides: "File-load orchestration exports for FIT/CSV flows"
      contains: "export async function processFitFile"
    - path: "frontend/src/shell/section3/section3Orchestration.ts"
      provides: "Section 3 detection/selection/trim orchestration exports"
      contains: "export async function runGpsLapDetection"
    - path: "frontend/src/shell/analysis/analyzeOrchestrator.ts"
      provides: "Analyze-button orchestration and parameter-change wiring"
      contains: "export async function handleAnalyze"
    - path: "frontend/src/shell/dem/demHandlers.ts"
      provides: "DEM source/file handling and results rendering delegation"
      contains: "export function displayResults"
    - path: "frontend/src/shell/app/initializeApplication.ts"
      provides: "Application shell composition and bootstrap wiring"
      contains: "export async function initializeApplicationShell"
    - path: "frontend/src/main.ts"
      provides: "Composition-root wiring only"
      contains: "initializeApplicationShell"
  key_links:
    - from: "frontend/src/main.ts"
      to: "frontend/src/shell/app/initializeApplication.ts"
      via: "composition-root dispatch delegates app bootstrap"
      pattern: "import.*initializeApplicationShell"
    - from: "frontend/src/shell/app/initializeApplication.ts"
      to: "frontend/src/shell/fileLoad/fileLoadOrchestration.ts"
      via: "FIT/CSV load handlers wired from composition entry"
      pattern: "processFitFile|processCsvFile|handleFileSelection"
    - from: "frontend/src/shell/app/initializeApplication.ts"
      to: "frontend/src/shell/analysis/analyzeOrchestrator.ts"
      via: "setupAnalyzeButton + handleAnalyze delegation"
      pattern: "setupAnalyzeButton|handleAnalyze"
    - from: "frontend/src/shell/app/initializeApplication.ts"
      to: "frontend/src/shell/section3/section3Orchestration.ts"
      via: "section-3 detection/selection handlers delegated to orchestration module"
      pattern: "runGpsLapDetection|runOutAndBackDetection|initializeSection3"
    - from: "frontend/src/shell/app/initializeApplication.ts"
      to: "frontend/src/shell/dem/demHandlers.ts"
      via: "DEM source selection/file load/results display delegated to dem handlers"
      pattern: "updateDEMSourceSelection|handleDEMFileSelection|displayResults"
    - from: "frontend/src/shell/analysis/analyzeOrchestrator.ts"
      to: "frontend/src/shell/analysis/renderDelegates.ts"
      via: "createModeRenderCallbacks for standard/gpsLap/outAndBack"
      pattern: "createModeRenderCallbacks"
---

<objective>
Complete Phase 5 closeout extraction so `frontend/src/main.ts` becomes a true composition root and no longer owns large residual shell behaviors.

Purpose: Satisfy CLOS-01 by implementing D-01 through D-12 exactly (aggressive extraction scope, structural-only lifts, behavior preservation, and seam-traceable MapVisualization posture).
Output: Residual `main.ts` functions moved into explicit shell modules, wiring delegated via imports, and regression validation evidence captured.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-closeout-secondary-cleanup-and-roadmap-sync/05-CONTEXT.md
@.planning/phases/05-closeout-secondary-cleanup-and-roadmap-sync/05-RESEARCH.md
@.planning/phases/05-closeout-secondary-cleanup-and-roadmap-sync/05-VALIDATION.md
@docs/testing/ui-shell-regression-contract.md
@docs/testing/ui-shell-manual-checklist.md
@scripts/validate-ui-shell-guardrails.sh
@scripts/report-ui-shell-hotspots.sh

<interfaces>
From frontend/src/shell/analysis/types.ts:
```typescript
export interface ShellServices {
  appState: AppState;
  showLoading: (message: string) => void;
  hideLoading: () => void;
  showError: (message: string) => void;
}

export interface ShellAnalysisContext extends ShellServices {
  waitForPlotly: () => Promise<void>;
}
```

From frontend/src/main.ts (current callback wiring pattern):
```typescript
const callbacks = createModeRenderCallbacks({
  standard: (args) => showVirtualElevationAnalysisInline(...),
  gpsLap: ({ lapIndexRanges, fitData, params, defaultAirSpeedOffset }) =>
    showGpsLapVEAnalysis({ appState, showLoading, hideLoading, showError }, parameterStorage, resultsStorage, waitForPlotly, lapIndexRanges, fitData, params, defaultAirSpeedOffset),
  outAndBack: ({ sections, fitData, params, defaultAirSpeedOffset }) =>
    showOutAndBackVEAnalysis({ appState, showLoading, hideLoading, showError }, parameterStorage, resultsStorage, sections, fitData, params, defaultAirSpeedOffset, waitForPlotly),
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract/rewire residual orchestration and fold helper lifts into the same surface (per D-01, D-02, D-03, D-04, D-05, D-10, D-11)</name>
  <files>
    frontend/src/main.ts
    frontend/src/shell/app/initializeApplication.ts
    frontend/src/shell/analysis/analyzeOrchestrator.ts
    frontend/src/shell/fileLoad/fileLoadOrchestration.ts
    frontend/src/shell/section3/section3Orchestration.ts
    frontend/src/shell/dem/demHandlers.ts
    frontend/src/components/MapVisualization.ts
  </files>
  <read_first>
    frontend/src/main.ts
    frontend/src/shell/analysis/renderDelegates.ts
    frontend/src/shell/analysis/prepareAnalysisPayload.ts
    frontend/src/shell/section3/bindGpsDetection.ts
    frontend/src/shell/section3/bindLapSelection.ts
    frontend/src/shell/section3/bindOutAndBackDetection.ts
    frontend/src/shell/app/initializeApplication.ts
    frontend/src/shell/analysis/analyzeOrchestrator.ts
    frontend/src/shell/fileLoad/fileLoadOrchestration.ts
    frontend/src/shell/section3/section3Orchestration.ts
    frontend/src/shell/dem/demHandlers.ts
    frontend/src/components/MapVisualization.ts
    .planning/phases/05-closeout-secondary-cleanup-and-roadmap-sync/05-CONTEXT.md
    .planning/phases/05-closeout-secondary-cleanup-and-roadmap-sync/05-RESEARCH.md
  </read_first>
  <action>
Implement aggressive extraction to satisfy D-01/D-02/D-03 exactly, with structural-only lifts (D-04), while minimizing file-surface expansion:

0. Fold residual helper/status/math extraction into the same orchestration files below (or `initializeApplication.ts`) rather than creating standalone helper modules.
   - Do NOT create plan-scoped utility files `frontend/src/shell/ui/formatters.ts`, `frontend/src/shell/ui/status.ts`, `frontend/src/shell/analysis/resultMath.ts`, or new barrel fan-out requirements.

1. Create/update `frontend/src/shell/fileLoad/fileLoadOrchestration.ts` and move these functions from `main.ts` with behavior preserved:
- `initializeFitProcessor`
- `handleFileSelection`
- `processFitFile`
- `processCsvFile`
- `displayFileInfo`
- `displayCsvResults`
- `initializeSection3Csv`
Keep BEHV-01 auto-scroll calls unchanged: `scrollToSection('parametersSection')` must still execute after successful FIT and CSV processing (D-05).

2. Create/update `frontend/src/shell/section3/section3Orchestration.ts` and move these functions:
- `runGpsLapDetection`
- `updateGpsDetectedLapsUI`
- `handleGpsLapSelectionChange`
- `runOutAndBackDetection`
- `updateOutAndBackSectionsUI`
- `handleOutAndBackSectionSelectionChange`
- `updateOutAndBackButtonState`
- `initializeMapTrimControls`
- `updateGpsMarkerButtonState`
- `updateSelectedLaps`
- `initializeMapTrimControlsForSelectedLaps`
- `initializeSection3`
- `isGpsLapSelectionMode`
- `getSelectedDataTimeRange`
- `findDataIndexAtTimeOffset`

3. Create/update `frontend/src/shell/dem/demHandlers.ts` and move:
- `updateDEMSourceSelection`
- `handleDEMFileSelection`
- `clearDEMFile`
- `displayResults`
Co-locate any required result-math helpers here (or in analyze orchestration) instead of introducing a dedicated `resultMath.ts` split.

4. Create/update `frontend/src/shell/analysis/analyzeOrchestrator.ts` and move:
- `handleAnalyze`
- `setupAnalyzeButton`
- `updateAnalyzeButton`
- `initializeAnalysisParameters`
- `handleParametersChange`
This module must keep using `prepareAnalysisPayload` + `createModeRenderCallbacks` and current `standard/gpsLap/outAndBack` delegate flow unchanged (D-03, D-05).

5. Create/update `frontend/src/shell/app/initializeApplication.ts` exporting:
- `initializeApplicationShell(...)` that composes dependencies and invokes the extracted modules.

6. Rewrite `frontend/src/main.ts` to composition-root posture (D-01):
- keep imports, DOM element acquisition, service construction (AppState, storages, MapVisualization, components), and top-level bootstrap dispatch only.
- remove local definitions for all functions moved above.
- no inline DOM template blocks or large orchestration function bodies remaining in `main.ts`.

7. `MapVisualization.ts` rule (D-10/D-11):
- touch `frontend/src/components/MapVisualization.ts` only if a concrete extracted seam requires a signature/export adjustment.
- if touched, restrict to interface narrowing + minimal behavior-neutral internal cleanup.
- do not perform lifecycle decomposition (MAP-01 remains deferred).

8. Preserve logging boundary: keep `log` usage; do not add raw `console.*`.

Per D-07, do not add any hard numeric main.ts ceiling to scripts/validate-ui-shell-guardrails.sh.
  </action>
  <verify>
    <automated>cd /Users/hannes/Documents/git/virtual-elevation-analyzer-web && rg -q "export async function processFitFile" frontend/src/shell/fileLoad/fileLoadOrchestration.ts && rg -q "export async function runGpsLapDetection" frontend/src/shell/section3/section3Orchestration.ts && rg -q "export async function handleAnalyze" frontend/src/shell/analysis/analyzeOrchestrator.ts && rg -q "export async function initializeApplicationShell" frontend/src/shell/app/initializeApplication.ts && rg -q "export (async )?function handleDEMFileSelection" frontend/src/shell/dem/demHandlers.ts && ! rg -q "function runGpsLapDetection|function processFitFile|function displayResults|async function handleAnalyze" frontend/src/main.ts</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/shell/fileLoad/fileLoadOrchestration.ts` contains `export async function processFitFile`
    - `frontend/src/shell/fileLoad/fileLoadOrchestration.ts` contains `export async function processCsvFile`
    - `frontend/src/shell/section3/section3Orchestration.ts` contains `export async function runGpsLapDetection`
    - `frontend/src/shell/section3/section3Orchestration.ts` contains `export function initializeSection3`
    - `frontend/src/shell/dem/demHandlers.ts` contains `export async function handleDEMFileSelection`
    - `frontend/src/shell/analysis/analyzeOrchestrator.ts` contains `export async function handleAnalyze`
    - `frontend/src/shell/app/initializeApplication.ts` contains `export async function initializeApplicationShell`
    - `frontend/src/main.ts` does NOT contain `function runGpsLapDetection`
    - `frontend/src/main.ts` does NOT contain `function processFitFile`
    - `frontend/src/main.ts` does NOT contain `function displayResults`
    - `frontend/src/main.ts` does NOT contain `async function handleAnalyze`
    - No new `console.` occurrences are added under `frontend/src/shell/`
    - Plan execution does not introduce dependencies on standalone `shell/ui/formatters.ts`, `shell/ui/status.ts`, or `shell/analysis/resultMath.ts`
  </acceptance_criteria>
  <done>All residual buckets are extracted through a narrowed file set; main.ts is materially smaller and composition-root oriented; behavior contracts remain structurally preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Run explicit D-06 CI parity suite before checkpoint</name>
  <files>
    frontend/package.json
  </files>
  <read_first>
    .planning/phases/05-closeout-secondary-cleanup-and-roadmap-sync/05-RESEARCH.md
    .planning/phases/05-closeout-secondary-cleanup-and-roadmap-sync/05-VALIDATION.md
  </read_first>
  <action>
Per D-06, execute the full CI parity sequence in-order and fail fast on first error. Use the exact command suite:
1. `cargo test --lib`
2. `wasm-pack build --target web --out-dir ../frontend/pkg`
3. `npm run check`
4. `npm run lint`
5. `npm run test`
6. `npm run build`
Record pass/fail evidence in the phase summary; do not proceed to Task 3 until this suite passes.
  </action>
  <verify>
    <automated>cd /Users/hannes/Documents/git/virtual-elevation-analyzer-web && cargo test --lib && wasm-pack build --target web --out-dir ../frontend/pkg && cd frontend && npm run check && npm run lint && npm run test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - D-06 CI parity suite is executed in the required order
    - `cargo test --lib` exits 0
    - `wasm-pack build --target web --out-dir ../frontend/pkg` exits 0
    - `npm run check` exits 0
    - `npm run lint` exits 0
    - `npm run test` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>D-06 CI parity is explicitly satisfied with command-level evidence prior to human regression checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual regression checkpoint for BEHV-01/BEHV-02/BEHV-03/BEHV-04 (per D-05, D-09, D-12)</name>
  <files>
    docs/testing/ui-shell-manual-checklist.md
    docs/testing/ui-shell-regression-contract.md
    .planning/phases/05-closeout-secondary-cleanup-and-roadmap-sync/05-01-SUMMARY.md
  </files>
  <read_first>
    docs/testing/ui-shell-manual-checklist.md
    docs/testing/ui-shell-regression-contract.md
    .planning/phases/05-closeout-secondary-cleanup-and-roadmap-sync/05-CONTEXT.md
    frontend/src/shell/fileLoad/fileLoadOrchestration.ts
    frontend/src/shell/analysis/analyzeOrchestrator.ts
    frontend/src/shell/section3/section3Orchestration.ts
  </read_first>
  <action>
Run browser verification after Task 1 extraction and Task 2 CI parity automation complete:

1. Start app: `cd frontend && npm run dev`.
2. Execute manual checklist in `docs/testing/ui-shell-manual-checklist.md` covering:
   - BEHV-01: auto-scroll to Analysis Parameters after FIT and CSV load.
   - BEHV-02: standard VE run and output parity.
   - BEHV-03: GPS in-place updates preserve active tab + scroll.
   - BEHV-04: GPS calibration behavior remains correct across gpsLap / gps gate one-way / out-and-back.
3. If any behavior drift appears, stop and report as regression (D-04).
4. For `MapVisualization.ts` touches: in `05-01-SUMMARY.md` record exact seam trigger + proof of no behavior change; if untouched, explicitly state "MapVisualization.ts not modified" (D-12).
  </action>
  <verify>
    <automated>cd /Users/hannes/Documents/git/virtual-elevation-analyzer-web/frontend && npm run build</automated>
    <manual>Complete `docs/testing/ui-shell-manual-checklist.md` and confirm all BEHV checks pass.</manual>
  </verify>
  <acceptance_criteria>
    - User confirms checklist approval with explicit "approved"
    - FIT load still auto-scrolls to `#parametersSection`
    - CSV load still auto-scrolls to `#parametersSection`
    - Standard VE analysis runs without visible output drift
    - GPS-lap and out-and-back in-place updates preserve active tab and scroll position
    - Auto-adjust calibration remains correct across GPS modes
    - `05-01-SUMMARY.md` contains explicit MapVisualization traceability statement (touched seam or untouched statement)
  </acceptance_criteria>
  <done>Manual regression contract is approved and closeout extraction is behavior-safe.</done>
  <what-built>Residual Phase 5 extraction completed; main.ts reduced to composition-root posture with shell-owned orchestration seams.</what-built>
  <how-to-verify>
1. `cd frontend && npm run dev`
2. Use a known FIT sample and a known CSV sample.
3. Validate BEHV-01: after successful load, viewport moves to Analysis Parameters.
4. Validate BEHV-02: run standard mode and confirm outputs/plots match expected baseline.
5. Validate BEHV-03: in GPS modes, interact with sliders + auto-adjust while on non-default tabs; tab and scroll must remain stable.
6. Validate BEHV-04: run auto-adjust and manual calibration in gpsLap and out-and-back; values and plots must update correctly.
7. Return `approved` if all checks pass; otherwise list exact failing step(s).
  </how-to-verify>
  <resume-signal>Type `approved` or provide the failing checklist step numbers and observed behavior.</resume-signal>
</task>

</tasks>

<verification>
- `bash scripts/validate-ui-shell-guardrails.sh --ci-only` exits 0
- D-06 CI parity suite runs in-order and all commands exit 0:
  - `cargo test --lib`
  - `wasm-pack build --target web --out-dir ../frontend/pkg`
  - `npm run check`
  - `npm run lint`
  - `npm run test`
  - `npm run build`
- `bash scripts/report-ui-shell-hotspots.sh` runs successfully and is used to review composition-root hotspot posture
- `rg "function runGpsLapDetection|function processFitFile|async function handleAnalyze" frontend/src/main.ts` returns no matches
- Manual checklist in `docs/testing/ui-shell-manual-checklist.md` is approved for BEHV-01/02/03/04
</verification>

<success_criteria>
`frontend/src/main.ts` is materially smaller and reads as composition root only, all residual buckets from D-02/D-03 are delegated to shell modules, and regression-sensitive behavior remains unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/05-closeout-secondary-cleanup-and-roadmap-sync/05-01-SUMMARY.md`
</output>
